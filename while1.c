// write a program that print 1 to 100
#include <stdio.h>
void main()
{
      int count = 1;
      int number;

      printf("enter number : ");
      scanf("%d",&number);

      while (count<=number)
      {
            printf("%d ", count);
            count = count + 1;
      }

      // count = count + 1;
      // printf("%d", count);

      // count = count + 1;
      // printf("%d", count);

      // count = count + 1;
      // printf("%d", count);

      // count = count + 1;
      // printf("%d", count);

      // count = count + 1;
      // printf("%d", count);

      // count = count + 1;
      // printf("%d", count);
      // count = count + 1;
      // printf("%d", count);
      // count = count + 1;
      // printf("%d", count);
      // count = count + 1;
      // printf("%d", count);
}