#include<stdio.h>
void main()
{
      // for all condition becomes true - and - &&
      // for any one condition becomes true - or - &&
      
      char latter;

      printf("enter a latter : ");
      scanf("%c",&latter);

      if(latter=='a' && latter=='e' && latter=='i' && latter=='o' && latter=='u')
      {
            printf("latter is vowel ");
      }

}